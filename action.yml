# SPDX-FileCopyrightText: 2026 Tanner Golden
# SPDX-License-Identifier: MIT
#
# The action metadata lives at the repository ROOT because GitHub Marketplace
# requires it there, and because `uses: tannergolden/trophies@v1` is the
# shortest thing a consumer has to type.
name: 'Trophies'
description: >-
  Trophies and achievements a GitHub profile or repository earns for itself:
  measured over the API on a schedule, drawn as committed SVGs, never fetched.
author: 'Tanner Golden'
branding:
  icon: 'award'
  color: 'yellow'

inputs:
  mode:
    description: >-
      run measures, renders, writes and reports; preview renders the built-in
      sample with no network; check redraws the committed case from the
      measurement the ledger remembers and fails if any file or the README
      block differs, with no token and no network.
    required: false
    default: 'run'
  case-mode:
    description: 'profile or repository. Empty reads .github/trophies.yml, whose default is profile.'
    required: false
    default: ''
  subject:
    description: >-
      Who to measure: a login in profile mode, owner/name in repository mode.
      Empty means the repository owner, or the repository the workflow runs in.
    required: false
    default: ''
  style:
    description: 'trophy, crest, medallion, crystal or plaque. Empty reads the config.'
    required: false
    default: ''
  config:
    description: 'Config file, relative to the repository root.'
    required: false
    default: '.github/trophies.yml'
  token:
    description: >-
      Token for the API. GITHUB_TOKEN sees public activity and everything
      about its own repository. A read-only personal token as TROPHIES_TOKEN
      also counts private contributions in profile mode.
    required: false
    default: ${{ github.token }}

outputs:
  changed:
    description: "'true' when the run modified any tracked file."
    value: ${{ steps.run.outputs.changed }}
  summary:
    description: 'One line: the case, the tiers, and how many achievements are earned.'
    value: ${{ steps.run.outputs.summary }}
  commit-title:
    description: 'The Conventional Commit subject for this run, unique to what it measured. Empty when nothing changed.'
    value: ${{ steps.run.outputs.commit-title }}
  commit-body:
    description: 'The commit body: the measured values, the date, the run, and why the images are committed.'
    value: ${{ steps.run.outputs.commit-body }}
  commit-file:
    description: 'Path of the full message, title and body, for `git commit -F`.'
    value: ${{ steps.run.outputs.commit-file }}

runs:
  using: 'composite'
  steps:
    # No setup-python step on purpose: the kit is stdlib-only and every
    # GitHub-hosted runner ships python3. Nothing to install, nothing to cache.
    - id: run
      shell: bash
      # Inputs travel through env, never interpolated into the script body -
      # a ${{ }} expansion inside `run:` is a shell-injection sink.
      env:
        KIT_MODE: ${{ inputs.mode }}
        KIT_CASE_MODE: ${{ inputs.case-mode }}
        KIT_SUBJECT: ${{ inputs.subject }}
        KIT_STYLE: ${{ inputs.style }}
        KIT_CONFIG: ${{ inputs.config }}
        KIT_PATH: ${{ github.action_path }}
        GITHUB_TOKEN: ${{ inputs.token }}
      run: |
        set -euo pipefail
        case "$KIT_MODE" in
          run|preview|check) ;;
          *) echo "::error::unknown mode '$KIT_MODE' (run|preview|check)"; exit 1 ;;
        esac
        PY="$(command -v python3 || command -v python || true)"
        if [ -z "$PY" ]; then echo "::error::no python3 or python on PATH"; exit 1; fi

        root="$GITHUB_WORKSPACE"
        # --untracked-files=all: the default collapses a new directory to one
        # "?? dir/" line, so a second SVG rendered into it would read as
        # "nothing changed".
        # The reusable workflow fetches this action beside the consumer's
        # checkout; keep that folder out of git's view so it never counts as
        # a change and never lands in a commit.
        if [ -d "$root/.trophies-kit" ] && [ -d "$root/.git" ]; then
          grep -qx '.trophies-kit/' "$root/.git/info/exclude" 2>/dev/null || echo '.trophies-kit/' >> "$root/.git/info/exclude"
        fi
        before="$(git -C "$root" status --porcelain --untracked-files=all 2>/dev/null || true)"

        args=( --root "$root" --config "$KIT_CONFIG" )
        if [ -n "$KIT_CASE_MODE" ]; then args+=( --mode "$KIT_CASE_MODE" ); fi
        if [ -n "$KIT_SUBJECT" ]; then args+=( --subject "$KIT_SUBJECT" ); fi
        if [ -n "$KIT_STYLE" ]; then args+=( --style "$KIT_STYLE" ); fi
        msg="$RUNNER_TEMP/trophies-commit.txt"
        rm -f "$msg"
        if [ "$KIT_MODE" = "run" ]; then
          args+=( --save "$RUNNER_TEMP/trophies-measurement.json" --commit-file "$msg" )
        fi

        out="$("$PY" "$KIT_PATH/src/trophy-kit.py" "$KIT_MODE" "${args[@]}" | tee /dev/stderr)"
        {
          echo "summary<<EOF"
          printf '%s\n' "$out" | head -n 1
          echo "EOF"
          # The commit message the kit wrote for this run: a Conventional
          # Commit whose subject and body carry the measured values, so no
          # two refreshes read alike. Empty when nothing changed.
          echo "commit-file=$msg"
          echo "commit-title<<EOF"
          if [ -f "$msg" ]; then head -n 1 "$msg"; fi
          echo "EOF"
          echo "commit-body<<EOF"
          if [ -f "$msg" ]; then tail -n +3 "$msg"; fi
          echo "EOF"
        } >> "$GITHUB_OUTPUT"

        after="$(git -C "$root" status --porcelain --untracked-files=all 2>/dev/null || true)"
        if [ "$before" != "$after" ]; then
          echo "changed=true" >> "$GITHUB_OUTPUT"
        else
          echo "changed=false" >> "$GITHUB_OUTPUT"
        fi
