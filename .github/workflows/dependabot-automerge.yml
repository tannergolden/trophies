# Installed from tannergolden/standards. Edit freely: this file is YOURS.
#
# Approves and queues Dependabot's patch and minor updates. The merge still
# waits on your required checks, so this removes the click and not the
# verification. Major bumps are left for a human on purpose.
#
# This is LIVE from the first commit. `.github/dependabot.yml` ships beside
# it with the `github-actions` ecosystem enabled, so Dependabot opens
# `build(deps):` pull requests for action updates and this workflow approves
# and queues them. GitHub only reads that file from the repository it belongs
# to, which is why it is installed here rather than shared like the logic.
#
# To review action bumps by hand instead, delete this file - the pull requests
# still arrive, they just wait for you.

name: '🦾 Automate Dependabot'

on:
  pull_request:
    types: [opened, reopened, synchronize]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: false

# ⚠️ THE `permissions:` BELOW IS A CEILING, NOT A GRANT. For a job that calls a
# reusable workflow, the caller's permissions cap what the called workflow may
# request. Anything it asks for beyond them fails the run before a single job
# starts: a startup failure, with no job, no step, and no log to read.
#
# They are written out rather than left to this repository's DEFAULT token
# permissions, because a repository set to read-only - now the common default -
# caps every write scope the same invisible way.
#
# The list is the union of what the called workflow's own jobs ask for, and
# each of those jobs still narrows it further from there. Widen it only to
# match a scope the called workflow actually declares.

permissions: {}

jobs:
  dependabot:
    if: ${{ !github.event.repository.is_template }}
    permissions:
      contents: write # allow the squash merge once required checks pass
      pull-requests: write # approve the pull request
    uses: tannergolden/standards/.github/workflows/dependabot-automerge.yml@v1
    secrets: inherit # zizmor: ignore[secrets-inherit] - first-party callee at the ref pinned above.
